{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title" : "Pelicula",
    "type": "object",
    "properties": {
     	"id": {"type": "integer"},
        "nombre": {"type": "string", 
        			"description": "nombre de la pelicula",
        			"maxLength": 255,
        			"minLength": 1
        },
        "duracion": {"type": "integer"}
    },
    "required": ["id", "nombre", "duracion"]
} 